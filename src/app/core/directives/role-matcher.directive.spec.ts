import { RoleMatcherDirective } from './role-matcher.directive';

describe('RoleMatcherDirective', () => {
  it('should create an instance', () => {
    const directive = new RoleMatcherDirective();
    expect(directive).toBeTruthy();
  });
});
